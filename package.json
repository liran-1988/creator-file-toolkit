{
  "name": "creator-file-toolkit",
  "version": "1.0.0",
  "private": true,
  "description": "Browser-only YouTube thumbnail validator and correction tool.",
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "test:browser": "node test/browser.cjs",
    "test:all": "npm test && npm run test:browser"
  },
  "devDependencies": {
    "playwright-core": "1.61.1"
  }
}
